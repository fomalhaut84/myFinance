import { Bot } from 'grammy'
import { replyHtml, escapeHtml, h } from '../utils/telegram'

const COMMANDS_HELP =
  `📌 ${h.b('사용 가능한 커맨드:')}\n` +
  `/start — 봇 소개\n` +
  `/help — 도움말\n\n` +
  `${h.b('한글 명령어')} (슬래시 없이 입력):\n` +
  `현황 — 전체 포트폴리오 요약\n` +
  `계좌 [이름] — 계좌 상세 (세진/소담/다솜)\n` +
  `주가 [종목] — 종목 현재가\n` +
  `환율 — USD/KRW 환율\n` +
  `매수 [계좌] [종목] [수량] [가격] — 매수 기록\n` +
  `매도 [계좌] [종목] [수량] [가격] — 매도 기록\n\n` +
  `${h.b('소비/수입 기록')} (자연어 입력):\n` +
  `점심 12000 — 소비 기록 + 카테고리 자동 분류\n` +
  `택시 4500 — 소비 기록\n` +
  `수입 월급 5000000 — 수입 기록\n\n` +
  `${h.b('가계부 조회:')}\n` +
  `소비 — 이번 달 소비 요약\n` +
  `수입 — 이번 달 수입 요약\n` +
  `예산 — 이번 달 예산 현황\n` +
  `예산설정 [금액] — 월 예산 설정\n\n` +
  `${h.b('AI 어드바이저:')}\n` +
  `/ai [질문] — AI에게 질문\n` +
  `자연어 질문도 가능 (예: "다솜 계좌 어때?")\n` +
  `자연어 거래도 가능 (예: "소담 AAPL 5주 250달러에 샀어")\n\n` +
  `${h.b('알림 설정:')}\n` +
  `알림설정 — 현재 알림 설정 조회\n` +
  `알림설정 [항목] [값] — 임계값 변경 (급락, 급등, 환율, 예산)\n\n` +
  `${h.b('전략 관리:')}\n` +
  `전략 [종목] — 현재 전략 조회\n` +
  `전략 [종목] [전략] — 전략 변경 (장기보유/스윙/모멘텀/가치/감시/단타)\n` +
  `전략목록 — 전체 종목 전략 현황\n\n` +
  `${h.b('관심종목:')}\n` +
  `관심 [종목] — 관심종목 추가\n` +
  `관심삭제 [종목] — 관심종목 제거\n` +
  `관심목록 — 관심종목 현황 + 현재가\n\n` +
  `${h.b('기술적 분석:')}\n` +
  `분석 [종목] — TA 리포트 (RSI, MACD, BB, SMA, 시그널)\n\n` +
  `${h.b('모닝 브리핑:')}\n` +
  `브리핑 — 모닝 브리핑 수동 발송\n` +
  `브리핑 한국/미국 — 특정 시장 브리핑`

export function registerCommands(bot: Bot): void {
  bot.command('start', async (ctx) => {
    const name = escapeHtml(ctx.from?.first_name ?? '사용자')
    await replyHtml(ctx,
      `안녕하세요, ${h.b(name)}님! 👋\n\n` +
        `myFinance 봇입니다.\n` +
        `포트폴리오 조회와 매매 기록을 도와드립니다.\n\n` +
        COMMANDS_HELP
    )
  })

  bot.command('help', async (ctx) => {
    await replyHtml(ctx, COMMANDS_HELP)
  })
}
